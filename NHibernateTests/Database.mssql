use [master]
go

drop database [NhibernateTests]
go

create database [NhibernateTests]
go

use [NhibernateTests]
go

create table [Items] (
    [Id] int not null identity(1, 1),
    [Name] varchar(64) not null,
    constraint [Items_PK] primary key ([Id])
)
go

exec sp_addlogin 
    @loginame = 'nhibLogin',
    @passwd = 'nhibPass'
    
exec sp_adduser
    @loginame = 'nhibLogin',
    @name_in_db = 'nhibUser',
    @grpname = 'db_owner'